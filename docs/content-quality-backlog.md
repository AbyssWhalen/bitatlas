# 内容质量清单（P1/P2）

> 建立时间：2026-09-03。来源：对 `apps/web/public/content/*.json` 全量 17 套题包（799 题）的
> 自动扫描 + 渲染页人工抽查。扫描方法见文末。当年已修复项保留记录以便回溯。

## 已修复（2026-09-03，commit 3d4df3e + 内容重建提交）

| 项 | 修复前 | 修复 |
| --- | --- | --- |
| P1 · 启动性能回归（15 套新题包加入后） | 每次启动串行重装 17 套题包 + 78MB 资产 17 路并行预热；全量 E2E 133/201 | 幂等跳过（sha256 不变不重写）、扩展年份后台安装、预热全局串行队列；E2E 201/201（run D） |
| P1 · 题干引用图但题包无图 | 2010-2025 共 89 题（扫描口径）做题时看不到图 | build-year 对题干命中图示关键词的题内嵌原卷页面图；重建后 2010-2025 共 97 题带题干图 |
| P2 · 形近错字（大千/小千/己知/由千/对千/己经） | 12 处（2010×4、2013×3、2015×1、2016×1、2017×1、2018×1、2023×1） | build-year 白名单规范化，重建后扫描 0 命中 |
| P2 · 解析文本丢失（生成器回归） | b801132 的 splitExplanations 正则丢失反斜杠，13 套题包解析全部退化为占位 | 修复正则；2012 +9、2014 +23、2015 +14、2018 +35 恢复真实解析；2010/2013/2017 保持 47/47 |

## 已修复（2026-09-03 第二批，解析标记形态扩展 + 2023 页码修复）

| 项 | 修复前 | 修复 |
| --- | --- | --- |
| P2 · 解析占位 · 标记形态不识别 | 530 题占位中的 342 题（2011/47、2016/47、2020/47、2022/47、2023/47 全占位；2012/38、2014/24、2015/33、2018/12 部分占位） | `splitExplanations` 标记正则扩展：`l/I` 误提取为 1（2011/2016）、数字与分隔符带空格（2012/2014/2016/2020）、双点 `N ..`（2015）、`解 析` 内空格（2018）、`NN.【解析】` 方括号与 `•` 分隔（2020）、`NN. X。【解析】` 答案字母夹层（2022）、`N.【参考答案】X + 块内【解析】`（2023）、分隔符后换行（2023 Q47）；块边界改取下一标记起点，去除旧版尾部粘连的 `N+1. 解析：` 尾巴（2010/2013/2017 共 54 处清理）。顺序门禁保持严格连续，真实数据 12 个有文本年份全部 47/47，无需容忍跳号 |
| P2 · 2023 答案页码虚增 | 2023 提取文本每页内嵌 `\f`，页码被算到 15-27（不存在），重建触发资产缺失校验失败 | join 前把页内 `\f` 归一为换行，只保留页边界分隔符；页码回到真实 1-14 |

重建后占位从 530 → 188（仅剩 2019/2021/2024/2025 四个无文本年份）。
单元测试 `build-year.node-test.mjs` 增至 17 例（新增 8 种标记形态 + 内嵌 `\f` 页码回归）。

## 已修复（2026-09-03 第三批，上标恢复 + 噪声裁剪 + 2020 Q47 截断 + 2012 Q1 代码残留）

| 项 | 修复前 | 修复 |
| --- | --- | --- |
| P2 · 上标丢失（7 题选项 + 全量解析内散布） | O(n²)→`O(n2)`、O(log₂n)→`O(log2n)`、O(n^½)→`O(n1/2)`/`O(n112)`、O(1)→`0(1)`（大写 O 被提取成数字 0） | `normalizeComplexityNotation`：仅处理括号内为纯复杂度形态（恰为 1 或 n/m/e/p/log/lenl 开头）的 `O/0/Θ/Ω(...)` 组；汇编 `0(t2)`、dB 公式 `10log10(S/N)`、位串、中文括注均不受影响。题干、选项、解析、提示统一过该管线；diff 审计 33 处变更全部符合预期 |
| P2 · 解析内嵌答案表噪声（4 题） | 2013 Q3/2015 Q2/2017 Q5 块尾吞入答案速查表；2016 Q1 表格与地址图残留夹在块中 | `trimExplanationNoise` + `EXPLANATION_NOISE_TRIMS` 按题人工核对锚点定点裁剪（from/to），锚点失配即构建失败（fail closed）；2016 Q1 保留其后真实正文 |
| 新发现 · 2020 Q47 外来试卷页 | 2020 答案卷末页附带 2019 年试卷首页，整页粘进 Q47 解析 | `splitExplanations` 块内出现「NNNN全国硕士研究生招生考试」试卷头即截断 |
| 新发现 · 2012 Q1 代码残留 | `return n*fact(n-10)`（`n-1)` 右括号被提取成 0，与解析「参数值减 1」矛盾） | 白名单新增 `fact(n-10)`→`fact(n-1)`（全库扫描仅此一处） |

重建后占位保持 188（仅 2019/2021/2024/2025 无文本年份），12 个有文本年份解析全部 47/47 无回归。
单元测试增至 23 例。

## 已修复（2026-09-03 第四批，扫描卷整页 OCR 解析恢复 188 → 80）

| 项 | 修复前 | 修复 |
| --- | --- | --- |
| P2 · 解析占位 · 无文本年份（2019/2021/2024/2025 共 188 题） | 答案卷为纯扫描图、无嵌入文本，`splitExplanations` 无从解析 | 新增 `tools/content-importer/src/ocr-answer-explanations.py`（项目本地 RapidOCR）整页离线识别：行聚类重组、页眉页脚剔除、水印片段过滤（`微信公众号计算机考研数据` / `985211` 等）、源 PDF 重复页按字节摘要去重（2024 第 4 页）、原始片段转储供审计；产物 `local-data/work/rebuild/<year>/answer-ocr-pages.json` 不入库 |
| P2 · OCR 标记形态不识别 | OCR 无空格紧凑标记 `1.解析：`、综合题 `NN.【答案要点】` | `splitExplanations` 支持紧凑标记与 `【答案要点】`；两阶段锚定：先严格从 1 递增，失败后允许任意首标记 + 严格 +1（2024/2025 综合题从 41 起） |
| P2 · OCR 复杂度记法全角/混搭括号 | `0（n）`、`O(1）`、`login`、`log 2 n`、`n 1/2` | `normalizeComplexityNotation` 支持全角/混搭括号与 OCR 形近归一（`login`→`log n`、`log 2 n`→`log₂n`、`n 1/2`→`n^(1/2)`、`n 2`→`n²`），保守白名单防误伤 |
| build-year OCR 集成 | 答案页无文本时整年退化为占位 | 答案页嵌入文本为空时按页号回填 `answer-ocr-pages.json` 的 OCR 文本，有嵌入文本的年份不受影响 |

重建后：2019/2021 恢复 47/47 真实解析（来源为逐题详解版答案卷，含选择题逐题解析）；
2024/2025 恢复 7 道综合题【答案要点】（来源参考答案仅含答案表 + 综合题要点，无逐题选择题解析）。
合计恢复 108 题解析 + 108 条首提示（hint[0] = 来源解析首句，与全库既有约定一致）。
其余 12 个有文本年份重建内容逐字节一致（diff 仅 `createdAt`/`sha256` 元数据），管线确定性无回归。
2025 Q44 `needs-review` 状态不变；CPU 实验 `parallel-5 / split-6` 保护项在实验代码中，不受题包重建影响。
单元测试 23 → 27 例（紧凑标记、【答案要点】、首标记锚定、全角括号归一）。
门禁：tsx 单测 27/27、lint、typecheck、Vitest 97 files / 1094 tests、content:validate 17 套、production build 全部通过。

## 未解项

### P2 · 解析占位（80 题，仅 2024/2025 选择题）

`"本题解析暂缺文字版，请通过来源页查看答案卷扫描件。"` 仍占位的题：
2024(40)、2025(40)，全部为单项选择题。

原因：2024/2025 官方参考答案卷只含答案表 + 综合题【答案要点】，
不存在逐题选择题解析文字；来源本身无内容可恢复。
兜底：练习页可打开来源页扫描件查看答案卷。

### P3 · OCR 恢复解析的逐字噪声（2019/2021/2024/2025）

2026-09-04 抽查：推理内容与答案键一致，但存在 OCR 字符级噪声——
`I/O`→`VO`（2019 Q24）、`帧`→`顿`（2021 Q47）、页码残片 `·019·` 混入正文、
全角/半角标点混杂。显示层可读、不影响作答与判定，且全部题包仍为
needs-review（人工复核时会覆盖到）。可在复核时顺手修订，或为
`answer-ocr-pages.json` 增加后处理规则（页码残片剔除、常见形近词表）。

### P2 · 提示词是通用模板（791/799 题）

第二提示全部为同一句「标出题干中的关键限制…」，无信息量（仅 2009 的 8 题有人工提示）。
方向：从知识点生成、或删去第二提示避免误导。

### P3 · 答案来源 locator 精度

13 套题包的答案 source.locator 为 `answer table (page 1 assumed)`（页码是推断）。
答案键本身已过「重构答案 == csgraduates 40/40」双源门禁，仅元数据精度问题。

### P3 · 题干配图为整页原图

题干内嵌的是整页原卷渲染图（含相邻题目）。裁剪出真实图示区域体验更好，
但需要逐题人工标注坐标，工作量大；整页图已可用。

### 已知扫描误报

`2025 Q6` 选项含「带权图中」被图示关键词扫描命中，实际无需配图（生成器按题干判定，未嵌图，正确）。

## PWA / 部署层（2026-09-03 巡检发现）

### 已修复 · SW 不接管已打开页面（commit 3e75f42）

线上实测：SW 激活后（即使经过在线 reload）`navigator.serviceWorker.controller` 恒为 null，
离线 reload 直接走网络失败（直连 3/3）。根因：generateSW 产物只有 `skipWaiting()` 没有
`clientsClaim()`。已在 vite.config.ts workbox 配置加 `clientsClaim: true`。

### P2 · sw.js 被 Cloudflare 边缘缓存

`/sw.js` 走 CF 边缘缓存（origin `Cache-Control: max-age=600`），部署后最长 10 分钟内
新访客仍拿旧 SW（实测带 query 绕过缓存可得新版，无 query 得旧版）。影响有限（skipWaiting +
prompt 更新流），但每次部署后 PWA 行为更新有 ≤10min 延迟。可选优化：CF Dashboard 加
Cache Rule 对 `/sw.js` 设 Bypass cache（需维护者操作，程序侧无法改 CF 配置）。

### P2 · precache 安装在慢网络下分钟级

88 项预缓存（约 2.7MB）在 SW install 时逐项拉取（`cache: 'reload'` 绕过 HTTP 缓存），
国内直连下实测需分钟级。期间离线不可用（SW 未 activated 或 precache 不完整）。
可选优化：`maximumFileSizeToCacheInBytes` 与 globPatterns 收敛（如 KaTeX 字体改 runtime
CacheFirst），缩短首装时间。

### 巡检结论（2026-09-03，国内直连）

- `https://408.fytjut.com/` 连续 5 次 probe 全部 200，TTFB ≈ 0.9-1.1s，直连稳定。
- 题包 JSON：CF `cf-cache-status: DYNAMIC`（不边缘缓存，回源 github.io），浏览器侧
  max-age=600 但应用以 no-store 自取；每次启动拉全量 JSON（≈2.8MB）属既有设计
  （更新检测 + sha256 校验），重复启动命中「未变化跳过重写」，不再重装 IndexedDB。
- 来源页 JPG：CF 边缘 HIT（max-age=14400），第二次起回源流量为零。

## 扫描方法

关键正则（`output/quality-scan.mjs`，本地未提交）：
- 图示引用：`/(下|上|如|右|左|该)图|图所示|图中|如图|示意图|流程图|题\s*\d+\s*图/`，配「题干无 image 块」判定；
- 解析占位：explanation 含 `暂缺` 或 `见来源页`；
- 形近错字：`大千/小千/等千/由千/对千/关千/位千/处千/千是/大干/.../己知/己经/己满/己分配/己排/己注/己完成` 白名单；
- 上标丢失：`/O\([a-z]+[0-9]\)|[a-z]1\/2\)|\^2|\^n|n\^|[0-9]\/2\)/`。

内容变更必须走 `tools/content-importer/src/build-year.mjs` 重建（`content:validate` 强制
published == generated），不得直接手改 `apps/web/public/content/*.json`。
